# ポモドーロタイマーアプリケーション 段階的実装計画

## フェーズ 1: 最小実用プロトタイプ（MVP）
目的: 画面と基本タイマー動作を最初に確認する

- `1.pomodoro/app.py` に Flask アプリファクトリを用意
- `1.pomodoro/templates/index.html` と `1.pomodoro/static/css/` / `1.pomodoro/static/js/` を用意
- フロントで以下を実装
  - タイマー開始 / 一時停止 / リセット
  - 作業 / 休憩の状態表示
  - ドーナツタイマー UI と `MM:SS` 表示
- タイマーはまずフロント単体で `Date.now()` 差分を使って動作させる
- API は静的 HTML だけ返す形で最小構成

成果:
- 画面表示ができる
- 直接ブラウザでタイマーが正しく動く
- UI/UX イメージが検証できる

---

## フェーズ 2: バックエンド API とデータ永続化
目的: タイマー完了記録と基本 API を追加する

- `models.py` に `Session` / `Settings` モデルを追加
- `config.py` で開発用設定と DB 設定を追加
- `services/` と `repositories/` の基本雛形を作成
- API エンドポイントを実装
  - `GET /api/status`
  - `POST /api/session`
  - `PUT /api/settings`
- フロントから `POST /api/session` でセッション完了を送信
- DB にセッション記録を保存し、次の動作で参照できるようにする

成果:
- セッション履歴がサーバーに記録される
- 設定更新 API が動く
- アプリのバックエンド構造が形成される

---

## フェーズ 3: 履歴・集計・設定管理
目的: ユーザー体験を強化し、実用的なデータ表示を追加する

- `GET /api/sessions?date=YYYY-MM-DD` で日別履歴取得
- フロントに「今日の進捗」カードを追加
  - 今日の完了数
  - 今日の合計集中時間
- 設定画面 / パネルを追加
  - 作業時間、短休憩、長休憩、長休憩までのサイクル数
- 設定保存 / 読み込みの UI/API連携
- フロントの状態遷移を整理
  - フェーズ切り替え（work -> short break -> long break）

成果:
- 履歴と集計が可視化される
- ユーザーがタイマー設定を変更できる
- 実用的なポモドーロ運用が可能になる

---

## フェーズ 4: テストと品質強化
目的: 安定性を高め、保守しやすい構成にする

- `tests/` に `unit/` / `integration/` を用意
- Flask で `create_app(config_name)` を使った `pytest` テスト
- service / repository 単体テストを追加
- JavaScript の `timerEngine.js` を純粋関数化し、`jest` / `jsdom` で単体テスト
- 時刻依存テストは `freezegun` / `sinon` などで安定化
- `features.md` と `architecture.md` を参照し、実装とドキュメントを一致させる

成果:
- アプリがテストカバレッジで守られる
- リファクタリングが安全にできる
- 追加機能の拡張余地が広がる

---

## フェーズ 5: 追加改善・運用対応
目的: 仕上げとしてユーザー体験と運用性を高める

- Web Worker によるタイマー計測を導入
- オフライン対応（`localStorage` / `IndexedDB` キュー）
- UI のアクセシビリティ改善（`aria-live` など）
- 本番構成の検討
  - `gunicorn` 起動
  - `nginx` または静的ファイル配信設定
- CI ワークフローでテストと静的解析を実行

成果:
- 本番対応を見据えた完成度
- UX と信頼性の向上
- 開発運用がスムーズになる

---

## 推奨する粒度
- フェーズは「MVP / API / データ表示 / テスト / 運用改善」の 5 段階
- 各フェーズは「1〜2 週間で完了できる小さめのまとまり"
- 最初は UI と動作確認を優先し、後から DB・API・設定・テストを積み上げる

この順番なら、最初に形が見え、途中で軌道修正しやすくなります。