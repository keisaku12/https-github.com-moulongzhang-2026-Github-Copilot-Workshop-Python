# ポモドーロタイマーアプリケーション 実装機能一覧

## 1. 基本タイマー機能
- ポモドーロセッションの開始
- ポモドーロセッションの一時停止
- ポモドーロセッションのリセット
- 作業タイマー / 短い休憩タイマー / 長い休憩タイマーの切り替え
- タイマー残り時間の表示（MM:SS）
- タイマー完了時の通知 / アラート

## 2. セッション管理
- セッション完了記録の保存
- セッションタイプ（work / break）の区別
- セッション開始時刻、終了時刻、継続時間の記録

## 3. ユーザー設定
- 作業時間の設定
- 短休憩時間の設定
- 長休憩時間の設定
- 長休憩までのサイクル数設定
- 設定の保存 / 読み込み

## 4. 履歴と集計
- 日別セッション履歴の取得
- 今日の完了数の表示
- 今日の合計集中時間の表示
- 過去ログの閲覧（例: 日付別履歴）

## 5. フロントエンド UI
- HTML/CSS による画面表示
- ドーナツタイマーや進捗表示
- 操作ボタンと状態切り替え
- アクセシビリティ対応（`aria-live` など）

## 6. タイマー実行ロジック
- JavaScript での純粋なタイマーエンジン実装
- Web Worker での時間計測による UI 負荷軽減
- `Date.now()` 差分による正確な時間管理

## 7. バックエンド API
- `GET /api/status`：設定と現在ステータスの取得
- `POST /api/session`：セッション完了の記録
- `GET /api/sessions?date=YYYY-MM-DD`：履歴の取得
- `PUT /api/settings`：設定の更新

## 8. データストア
- SQLite + SQLAlchemy での永続化
- `Session` モデルの定義
- `Settings` モデルの定義

## 9. アプリ構成とテスト
- `create_app(config_name)` を使ったアプリファクトリ
- サービス層 / リポジトリ層の分離
- Python 単体テスト（`pytest`）
- JavaScript 単体テスト（`jest` / `jsdom`）
- Flask テストクライアント + テスト DB による統合テスト
- 時刻依存テストの安定化

## 10. 追加改善 / 運用
- オフライン対応（`localStorage` / `IndexedDB`）
- 本番向け構成（`gunicorn`, `nginx` など）
- CI でのテスト実行・静的解析